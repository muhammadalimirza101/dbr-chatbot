/**
 * Connector entrypoint — Phase 3 wires the Baileys transport and the
 * localhost HTTP server here. Phase 0 is scaffold only.
 */

console.log("DBR connector scaffold — implementation lands in Phase 3.");
